load("@npm//:defs.bzl", "npm_link_all_packages")
load("@npm//typescript:typescript/package_json.bzl", "bin")

npm_link_all_packages(name = "node_modules")

bin.tsc(
    name = "typecheck",
    srcs = ["app.ts"],
    args = ["app.ts"],
)
